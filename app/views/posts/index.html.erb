<div class="jumbotron">
	<h1 class="display-4">ようこそnekozeへ</h1>
	<!-- <p class="lead">nekozeは猫好きのためのSNSです。</p> -->
	<p class="lead">あなたの大切な猫や街で見かけた猫を撮影してみんなで共有しよう</p>
</div>

<div class="container">
	<% if @q.present? %>
		<h5 class="search-content bg bg-light"><%= @search.title_or_catname_cont %> : の検索結果</h5>
		<h5 class="search-count bg bg-light"><%= @search_posts.total_count %>件</h5>
	<% end %>
			<div class= "row">
				 <div class="col-sm-4 col-md-3">
					<%= render 'posts/rank', ranks: @rank  %>
				</div>

				<div class="col-sm-8 col-md-9">
					<br>
					<br>
					<br>
					<br>
					<div class= "row">

						<% @search_posts.each do |post| %>
							<div class="col-sm-6 col-md-5 col-lg-4">
								<div class="card mb-3">
									<div class="card-header">
										投稿者: <%= link_to post.user.name, user_path(post.user) %>
									</div>

									<div class="card-body">
										<p><%= post.title %></p>
										<%= link_to post_path(post) do %>
											<%= attachment_image_tag post, :image, :fill, 200, 200, class: "img-fluid" %>
										<% end %>
										<% if post.catname.present? %>
										<p><span class="bg bg-success text-white" ><%= post.catname %></span></p>
										<% end %>
									</div>
								</div>
							</div>
						<% end %>
					</div>
				</div>
			</div>
			<div class="d-flex justify-content-end">
			<%=paginate @posts, class: "paginate" %>
			</div>
</div>
